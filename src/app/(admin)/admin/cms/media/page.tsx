import MediaClient from "./MediaClient";

export const metadata = {
  title: "Medya Yönetimi | XipSoft Admin",
  robots: { index: false, follow: false },
};

export default function MediaPage() {
  return <MediaClient />;
}
